server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /eventus/api/ {
        proxy_pass ${EVENTUS_API_URL}/eventus/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
